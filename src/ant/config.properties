config.bindir=${config.prefix}/bin
config.class.path=
config.datadir=${config.prefix}/share
config.docdir.package=${config.docdir}/${project.name}
config.docdir=${config.datadir}/doc/packages
config.javadir=${config.datadir}/java
config.javadocdir.java=${config.javadocdir}/java
config.javadocdir.package=${config.javadocdir}/${project.name}
config.javadocdir=${config.datadir}/javadoc
config.jnidir=${config.libdir}/java
config.libdir=${config.prefix}/lib64
config.libexecdir=${config.prefix}/lib
config.licensedir.package=${config.licensedir}/${project.name}
config.licensedir=${config.datadir}/licenses
config.mandir=${config.datadir}/man
config.mavenpomsdir=${config.datadir}/maven-poms
config.prefix=/usr
config.sysconfdir=/etc
project.description=Commandline processing for Java${line.separator}
project.developer.email=shvetz.anton@gmail.com
project.developer.id=urbic
project.developer.name=Anton Shvetz
project.developer.timezone=Europe/Moscow
project.developer.url=https://github.com/${project.developer.id}
project.license.name=Zlib
project.license.url=https://opensource.org/licenses/${project.license.name}
project.maven.artifactId=${project.name}
project.maven.groupId=coneforest
project.maven.version=${project.version}
project.moduleprefix=${project.maven.groupId}.${project.maven.artifactId}
project.name=${ant.project.name}
project.purl=pkg:github/${project.developer.id}/${project.name}
project.scm.connection=scm:git:git@github.com:${project.developer.id}/${project.name}.git
project.scm.developerConnection=scm:git:git@github.com:${project.developer.id}/${project.name}.git
project.scm.url=https://github.com/${project.developer.id}/${project.name}
project.summary=Commandline processing for Java
project.url=https://${project.developer.id}.github.io/${project.name}
project.version.major=0
project.version.minor=1
project.version.micro=3
project.version.type=
project.version=${project.version.major}.${project.version.minor}.${project.version.micro}
